<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MarcianoApp" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/vermelho</item>
        <item name="colorPrimaryDark">@color/vermelho_escuro</item>
        <item name="colorAccent">@color/vermelho</item>
        <item name="android:windowBackground">@color/fundo</item>
    </style>

    <style name="Theme.MarcianoApp" parent="Base.Theme.MarcianoApp" />
</resources>